@{
    ViewData["Title"] = "Xác nhận gửi email";
    Layout = "_AuthLayout";
}

<div class="text-center">
    <div class="mb-4">
        <i class="bi bi-envelope-check display-1 text-success"></i>
    </div>

    <h2 class="auth-title">Email đã được gửi</h2>
    <p class="auth-subtitle">
        Chúng tôi đã gửi hướng dẫn đặt lại mật khẩu đến địa chỉ email:
        <strong>@ViewBag.Email</strong>
    </p>

    <div class="alert alert-info">
        <i class="bi bi-info-circle me-2"></i>
        Vui lòng kiểm tra hộp thư đến (và cả thư mục spam) để tìm email từ chúng tôi.
    </div>

    <div class="mt-4">
        <p class="text-muted small">
            Không nhận được email?
            <a href="@Url.Action("Index", "ForgotPassword")" class="text-decoration-none">
                Gửi lại
            </a>
        </p>
    </div>
</div>

<div class="auth-links">
    <div class="text-center">
        <a href="@Url.Action("Index", "Login")">
            <i class="bi bi-arrow-left me-1"></i>
            Quay lại đăng nhập
        </a>
    </div>
</div>
